# 2026-03-24 工作日志

## Excel 数据处理网页工具

- 构建了纯前端 HTML 单页应用 `excel-processor.html`
- 功能：上传 xlsx/xls → 随机删除行（按保留数量 / 按删除数量）→ 删除 F 列之后的所有列（需手动确认）→ 导出处理后的文件
- 技术栈：原生 HTML/CSS/JS + SheetJS (xlsx@0.18.5 CDN)
- 设计风格：暗色主题、玻璃态卡片、优质动效
- 文件路径：`d:\software\codebuddy-int\Codex\excel-processor.html`

## 修复（17:56）
- 行操作新增**按比例(%)模式**，保留/删除都支持「按数量」和「按比例」两种模式切换
- 列删除逻辑修正：从 **F列（索引5）开始删除**，只保留 A-E 共5列（原逻辑错误保留了6列含F）
- KEEP_COLS 常量化为 5，便于后续维护

## 修复（18:03）
- 行操作互斥：保留/删除一旦有一个执行，另一个卡片自动 disabled（半透明遮罩）
- 下载防护：文件载入后下载按钮为锁定态，必须至少执行一次行操作或列操作后才解锁
- `hasExecuted` + `rowOpChosen` 两个状态变量控制上述逻辑
